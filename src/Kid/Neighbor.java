package Kid;

public class Neighbor {
    public static void main(String[] args) {
        Kid k = new Kid();
////  @todo once the subclass-outside-the-package inherites the protected member, that member(as inherited by the
////   subclass) becomes private to any code outside the subclass.
//        System.out.println(k.x);
    }
}
